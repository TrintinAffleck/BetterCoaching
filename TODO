When I use mouse button to go back the message is not read in inbox
Probably could cleanup a lot of views by adding code into utils.py
Implement updateCoach view
Add unread messages to the nav bar if there is a new message
Fix Masters+ Ranks to not show division
Fix header menu items to be all the same size
After leaving a coach review refreshing posts the same review
Fix coach cards to be all the same size
Change all html files to bootstrap stuff
Add a remember me checkbox to the login page